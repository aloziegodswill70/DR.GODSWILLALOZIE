// app/viewport.js
export default function viewport() {
  return {
    // required by Next 16+ (move viewport/themeColor here)
    viewport: "width=device-width, initial-scale=1",
    themeColor: "#C6A667"
  };
}
